apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: todolist
  labels:
    app: todolist
    component: backend
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
